SELECT C.CUSTOMER_ID, C.CUSTOMER_NAME 
FROM CUSTOMER C
JOIN (
	SELECT DISTINCT CUSTOMER_ID FROM ORDERT
	WHERE ORDER_DATE > '2020-01-10' AND ORDER_DATE <= '2020-01-15 23:59:59'
) AS C2 
ON C.CUSTOMER_ID = C2.CUSTOMER_ID
